import 'package:flutter/material.dart';

// Notifications page
class NotificationsPage extends StatelessWidget {
  const NotificationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      padding: const EdgeInsets.all(10),
      children: [
        const Padding(
          padding: EdgeInsets.symmetric(vertical: 0.0),
          child: Text(
            'Trending',
            style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
        Card(
          margin: EdgeInsets.symmetric(vertical: 7.0),
          child: ListTile(
            dense: true,
            leading: const Icon(Icons.person),
            title: const Text('This is one of the notifications'),
            trailing: const Icon(Icons.arrow_forward_ios),
          ),
        ),
      ],
    );
  }
}
